{
  "name": "spine-js-client",
  "version": "0.0.1",
  "private": true,
  "description": "JS implementation of the Spine client.",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile": "webpack --config webpack.config.js --mode=production",
    "test": "nyc --reporter=text-lcov mocha --require babel-polyfill --require babel-register --recursive $NODE_DEBUG_OPTION --exit --full-trace ./test > build/coverage.lcov"
  },
  "dependencies": {
    "base64-js": "^1.3.0",
    "firebase": "^4.12.1",
    "google-protobuf": "^3.5.0",
    "isomorphic-fetch": "^2.2.1",
    "uuid": "^3.2.1"
  },
  "devDependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0",
    "codecov": "^3.0.0",
    "mocha": "^5.1.0",
    "nyc": "^11.7.1",
    "webpack": "^4.5.0",
    "webpack-cli": "^2.0.14"
  }
}
