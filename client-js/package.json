{
  "name": "spine-js-client",
  "version": "$PACKAGE_VERSION",
  "license": "Apache-2.0",
  "description": "JS implementation of the Spine client.",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "coverage": "nyc --reporter=text-lcov $PACKAGE_TEST > ../coverage.lcov",
    "test": "$PACKAGE_TEST"
  },
  "dependencies": {
    "base64-js": "^1.3.0",
    "firebase": "^4.12.1",
    "isomorphic-fetch": "^2.2.1",
    "spine-js-client-proto": "$PACKAGE_VERSION",
    "uuid": "^3.2.1"
  },
  "devDependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-register": "^6.26.0",
    "codecov": "^3.0.0",
    "mocha": "^5.1.0",
    "nyc": "^11.7.1",
    "google-protobuf": "$PACKAGE_PROTOBUF_VERSION"
  }
}
